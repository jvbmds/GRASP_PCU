#pragma once

#include <vector>

struct registro {           // Struct que armazena informacao dos itens
    int demanda;		    // Demanda total do item
    int tamanho;            // Largura do item.
    int indice;				//Indice original da peca.
};